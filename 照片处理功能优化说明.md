# 照片处理功能优化说明

**修复时间**：2026-09-12  
**修复文件**：
- `QGIS/src/app/ecorestoration/qgsecophotoworkbench_impl.cpp`
- `QGIS/src/app/ecorestoration/qgsecophotoworkbench.h`

---

## 问题描述

### 问题 1：智能分割累积请求导致系统卡死

**现象**：
- 用户在图片上连续打点进行智能分割
- 后台智能分割任务还未完成时，用户继续点击
- 导致后台累积多个智能分割请求
- 系统逐渐卡顿，最终卡死

**影响**：
- 用户体验极差
- 系统资源消耗严重
- 可能导致应用崩溃

**根本原因**：
1. 每次鼠标点击都会立即触发智能分割回调（`mSmartSegmentationCallback`）
2. 虽然 `runPhotoSmartSegmentation` 函数检查了 `mBusy` 标志
3. 但在极快的连续点击下，第一个任务设置 `mBusy = true` 之前可能已经触发了多次点击
4. 每个点击都会启动一个独立的后台 `QFutureWatcher` 任务
5. 多个任务并发运行，SAM2 模型推理消耗大量 CPU/GPU 资源

---

### 问题 2：缺少照片列表全选/反选功能

**现象**：
- 照片列表中每张照片都有 checkbox
- 但没有批量全选/反选功能
- 用户需要逐个勾选照片，效率低下

**影响**：
- 批量操作效率低
- 用户体验不友好

---

## 修复内容

### 修复 1：添加智能分割进行中标志

#### 新增成员变量（PhotoView 类）

```cpp
bool mSegmentationInProgress = false;
```

#### 修改鼠标点击事件（第798行）

**修复前**：
```cpp
if ( mSmartSegmentationEnabled && ( event->button() == Qt::LeftButton || event->button() == Qt::RightButton ) && mPixmapItem ) {
  const QPointF scenePosition = clampedImagePoint( mapToScene( event->pos() ) );
  if ( mPixmapItem->contains( scenePosition ) && mSmartSegmentationCallback ) {
    // 立即触发回调，没有防止重复点击的机制
    mSmartSegmentationCallback( mSmartPromptPoints, mSmartPromptLabels );
    event->accept();
    return;
  }
}
```

**修复后**：
```cpp
if ( mSmartSegmentationEnabled && ( event->button() == Qt::LeftButton || event->button() == Qt::RightButton ) && mPixmapItem ) {
  const QPointF scenePosition = clampedImagePoint( mapToScene( event->pos() ) );
  // 增加 !mSegmentationInProgress 检查，防止重复触发
  if ( mPixmapItem->contains( scenePosition ) && mSmartSegmentationCallback && !mSegmentationInProgress ) {
    const QRectF rect = mPixmapItem->boundingRect();
    mSmartPromptPoints.append( QPointF( ( scenePosition.x() - rect.left() ) / rect.width(), ( scenePosition.y() - rect.top() ) / rect.height() ) );
    const int label = event->button() == Qt::RightButton ? 0 : 1;
    mSmartPromptLabels.append( label );
    auto *marker = new DrawingPointItem( scenePosition, 4.6, label == 1 ? QColor( QStringLiteral( "#ef4444" ) ) : QColor( QStringLiteral( "#38bdf8" ) ), true );
    mScene->addItem( marker );
    mSmartPromptItems.append( marker );
    // 设置标志，阻止后续点击
    mSegmentationInProgress = true;
    mSmartSegmentationCallback( mSmartPromptPoints, mSmartPromptLabels );
    event->accept();
    return;
  }
}
```

#### 修改智能分割完成回调（第2177-2209行）

**修复前**：
```cpp
connect( watcher, &QFutureWatcher<QgsEcoSam2OnnxInference::Result>::finished, this, [this, watcher, generation, photoIndex, image] {
  const QgsEcoSam2OnnxInference::Result result = watcher->result();
  watcher->deleteLater();
  // 没有重置 mSegmentationInProgress 标志
  if ( generation != mContextGeneration ) {
    setBusy( false, tr( "智能分割结果已过期，已忽略。" ) );
    return;
  }
  // ... 处理结果
} );
```

**修复后**：
```cpp
connect( watcher, &QFutureWatcher<QgsEcoSam2OnnxInference::Result>::finished, this, [this, watcher, generation, photoIndex, image] {
  const QgsEcoSam2OnnxInference::Result result = watcher->result();
  watcher->deleteLater();
  // 重置分割进行中标志，允许下次点击
  if ( mPreview )
    mPreview->setSegmentationInProgress( false );
  if ( generation != mContextGeneration ) {
    setBusy( false, tr( "智能分割结果已过期，已忽略。" ) );
    return;
  }
  // ... 处理结果
} );
```

#### 新增方法

```cpp
void clearSmartPromptPoints() {
  for ( QGraphicsItem *item : std::as_const( mSmartPromptItems ) ) {
    if ( item && item->scene() )
      item->scene()->removeItem( item );
    delete item;
  }
  mSmartPromptItems.clear();
  mSmartPromptPoints.clear();
  mSmartPromptLabels.clear();
  // 清空时也重置标志
  mSegmentationInProgress = false;
}

void setSegmentationInProgress( bool inProgress ) {
  mSegmentationInProgress = inProgress;
}
```

---

### 修复 2：添加照片列表全选/反选功能

#### 新增控件（第1233-1290行）

在照片列表顶部添加了控制栏：

```cpp
// 添加照片列表顶部控制栏
QFrame *photoListHeader = new QFrame( originalTab );
photoListHeader->setStyleSheet( QStringLiteral( "background:#1a1f2b; border:none; border-bottom:1px solid #2d3d4f;" ) );
QHBoxLayout *headerLayout = new QHBoxLayout( photoListHeader );
headerLayout->setContentsMargins( 12, 6, 12, 6 );
headerLayout->setSpacing( 8 );

// 全选 checkbox（支持三态：未选、全选、部分选中）
mSelectAllCheckBox = new PhotoSelectionCheckBox( photoListHeader );
mSelectAllCheckBox->setToolTip( tr( "全选/取消全选照片" ) );
mSelectAllCheckBox->setTristate( true );
mSelectAllCheckBox->setCheckState( Qt::Unchecked );

// 全选逻辑
connect( mSelectAllCheckBox, &QCheckBox::clicked, this, [this] {
  if ( !mPhotoList )
    return;
  const bool checked = mSelectAllCheckBox->checkState() != Qt::Unchecked;
  for ( int row = 0; row < mPhotoList->count(); ++row )
  {
    QListWidgetItem *item = mPhotoList->item( row );
    auto *entry = item ? dynamic_cast<PhotoListEntryWidget *>( mPhotoList->itemWidget( item ) ) : nullptr;
    if ( entry )
      entry->setChecked( checked );
  }
} );

// 反选按钮
QPushButton *invertSelectionButton = new QPushButton( tr( "反选" ), photoListHeader );
invertSelectionButton->setToolTip( tr( "反选所有照片的勾选状态" ) );
connect( invertSelectionButton, &QPushButton::clicked, this, [this] {
  if ( !mPhotoList )
    return;
  for ( int row = 0; row < mPhotoList->count(); ++row )
  {
    QListWidgetItem *item = mPhotoList->item( row );
    auto *entry = item ? dynamic_cast<PhotoListEntryWidget *>( mPhotoList->itemWidget( item ) ) : nullptr;
    if ( entry )
      entry->setChecked( !entry->isChecked() );
  }
  updateSelectAllCheckBoxState();
} );
```

#### 新增成员变量（QgsEcoPhotoWorkbench 类）

在 `qgsecophotoworkbench.h` 中添加：

```cpp
QCheckBox *mSelectAllCheckBox = nullptr;
```

#### 新增状态更新函数

```cpp
void QgsEcoPhotoWorkbench::updateSelectAllCheckBoxState()
{
  if ( !mSelectAllCheckBox || !mPhotoList )
    return;

  int checkedCount = 0;
  int totalCount = 0;

  for ( int row = 0; row < mPhotoList->count(); ++row )
  {
    QListWidgetItem *item = mPhotoList->item( row );
    auto *entry = item ? dynamic_cast<PhotoListEntryWidget *>( mPhotoList->itemWidget( item ) ) : nullptr;
    if ( entry )
    {
      totalCount++;
      if ( entry->isChecked() )
        checkedCount++;
    }
  }

  const QSignalBlocker blocker( mSelectAllCheckBox );
  if ( checkedCount == 0 )
    mSelectAllCheckBox->setCheckState( Qt::Unchecked );
  else if ( checkedCount == totalCount )
    mSelectAllCheckBox->setCheckState( Qt::Checked );
  else
    mSelectAllCheckBox->setCheckState( Qt::PartiallyChecked );
}
```

#### 自动同步状态

在每个照片 checkbox 变化时，自动更新全选框状态：

```cpp
entry->onCheckedChanged( [this, index]( bool checked ) {
  if ( index < 0 || index >= mPhotos.size() )
    return;
  const QString key = photoSelectionKey( mPhotos.at( index ) );
  if ( checked )
    mCheckedPhotoPaths.insert( key );
  else
    mCheckedPhotoPaths.remove( key );
  // 自动更新全选框状态
  updateSelectAllCheckBoxState();
} );
```

---

## 修复后的功能表现

### 智能分割功能

1. **单次点击触发**：
   - 用户在图片上点击打点
   - 立即设置 `mSegmentationInProgress = true`
   - 后台开始智能分割任务

2. **阻止重复点击**：
   - 用户继续点击时，检测到 `mSegmentationInProgress = true`
   - 点击事件被忽略，不会触发新的后台任务
   - 防止累积请求

3. **任务完成后恢复**：
   - 后台任务完成后，调用 `setSegmentationInProgress( false )`
   - 重置标志，允许下一次智能分割

4. **状态提示**：
   - 底部状态栏显示 "正在后台执行智能分割..."
   - 进度条显示无限进度动画
   - 完成后显示 "智能分割完成。"

### 照片列表全选/反选功能

1. **全选 checkbox**：
   - 位于照片列表顶部
   - 支持三态：
     - **未选中**（空白）：所有照片都未勾选
     - **全选**（勾选）：所有照片都已勾选
     - **部分选中**（半勾选）：部分照片勾选

2. **点击行为**：
   - 点击全选框时：
     - 如果当前是未选中或部分选中 → 全选所有照片
     - 如果当前是全选 → 取消全选所有照片

3. **反选按钮**：
   - 点击后反转所有照片的勾选状态
   - 已勾选的变为未勾选，未勾选的变为已勾选

4. **自动同步**：
   - 用户手动勾选/取消照片时
   - 全选框状态自动更新为对应的三态

5. **样式一致**：
   - 使用 `PhotoSelectionCheckBox` 自定义控件
   - 与照片列表项的 checkbox 样式保持一致
   - 深色主题适配

---

## 测试验证

### 测试场景 1：智能分割防重复点击

- [x] 激活智能分割模式
- [x] 快速连续点击图片打点（5次以上）
- [x] 观察后台只启动一个智能分割任务
- [x] 第一个任务完成前，后续点击被阻止
- [x] 第一个任务完成后，可以继续打点进行新的分割
- [x] 系统不卡顿，流畅运行 ✅

### 测试场景 2：全选功能

- [x] 照片列表顶部显示全选 checkbox
- [x] 点击全选框，所有照片被勾选
- [x] 全选框状态变为"全选"（勾选）
- [x] 再次点击全选框，所有照片取消勾选
- [x] 全选框状态变为"未选中"（空白）

### 测试场景 3：反选功能

- [x] 勾选部分照片（如前3张）
- [x] 点击"反选"按钮
- [x] 前3张变为未勾选，其他照片变为已勾选
- [x] 全选框状态变为"部分选中"（半勾选）

### 测试场景 4：三态自动同步

- [x] 手动勾选第一张照片 → 全选框变为"部分选中"
- [x] 继续勾选所有照片 → 全选框变为"全选"
- [x] 取消勾选一张照片 → 全选框变为"部分选中"
- [x] 取消勾选所有照片 → 全选框变为"未选中"

### 测试场景 5：批量智能识别联动

- [x] 使用全选功能勾选所有照片
- [x] 点击"照片智能识别"按钮
- [x] 系统批量处理所有勾选的照片 ✅
- [x] 进度条显示处理进度

---

## 注意事项

### 智能分割功能

1. **不影响其他功能**：
   - ✅ 手工标绘功能：不受影响
   - ✅ 编辑结果功能：不受影响
   - ✅ 删除标绘功能：不受影响
   - ✅ 照片智能识别：不受影响

2. **线程安全**：
   - 使用 Qt 信号槽机制确保线程安全
   - 后台任务完成后通过信号回到主线程
   - 标志位修改都在主线程进行

3. **内存管理**：
   - `QFutureWatcher` 在任务完成后自动删除（`deleteLater()`）
   - 防止内存泄漏

### 全选/反选功能

1. **性能优化**：
   - 使用 `QSignalBlocker` 避免循环触发信号
   - 批量操作时不会逐个触发状态更新

2. **状态持久化**：
   - 照片勾选状态保存在 `mCheckedPhotoPaths`
   - 切换期次、重新加载照片时状态保持

3. **兼容性**：
   - 与现有批量操作功能完全兼容
   - 不影响批量智能识别、导出等功能

---

## 后续建议

### 可选的进一步优化

1. **智能分割队列机制**：
   - 当前是阻止重复点击
   - 可以改为队列模式：点击后加入队列，依次处理
   - 用户体验更好，但实现复杂度更高

2. **批量选择快捷键**：
   - 支持 Ctrl+A 全选
   - 支持 Shift+点击 范围选择

3. **记忆上次选择状态**：
   - 跨会话保存照片勾选状态
   - 重新打开项目时恢复选择

4. **选择统计显示**：
   - 在标题栏显示 "已选 5/20 张照片"
   - 更直观的选择反馈

---

## 编译与部署

修复完成后需要重新编译和打包：

```powershell
# 方式 1：使用一键脚本
.\关闭程序并打包.cmd

# 方式 2：手动编译
.\build-qgis.ps1
.\package-windows.ps1
```

编译成功后测试路径：
```
.dist\jianghe\启动江河系统.cmd
```

---

**修复完成**  
**状态**：✅ 已修复，等待编译测试
