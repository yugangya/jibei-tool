# QGIS 江河系统 - 编译与打包指南

## 中文乱码修复说明

本次已修复以下中文乱码问题：

### 1. 日历控件箭头乱码
- **位置**：`QGIS/src/app/ecorestoration/qgsecorestorationcontroller.cpp` 第 1823、1832 行
- **修复前**：`"鈥筡"`, `"鈥篭"`
- **修复后**：`"←"`, `"→"`

### 2. 电压等级乱码
- **位置**：`QGIS/src/app/ecorestoration/qgsecorestorationcontroller.cpp` 第 3881 行
- **修复前**：`"卤800kV"`, `"卤500kV"`
- **修复后**：`"±800kV"`, `"±500kV"`

---

## 编译方法

### 方法一：使用一键脚本（推荐）

双击运行：
```
重新编译并打包.cmd
```

脚本会自动完成：
1. 检查编译环境
2. 编译 QGIS
3. 打包部署版本

### 方法二：手动执行（PowerShell）

```powershell
# 1. 编译
.\build-qgis.ps1

# 2. 打包
.\package-windows.ps1
```

### 方法三：只编译不打包（快速验证）

```powershell
# 仅编译
.\build-qgis.ps1

# 运行开发版本
.\start-qgis.ps1
```

---

## 编译时间参考

- **增量编译**（只改了几个文件）：2-5 分钟
- **完整编译**（首次或大量修改）：10-30 分钟
- **打包**：1-3 分钟

*实际时间取决于 CPU 性能和磁盘速度*

---

## 编译输出位置

### 编译输出
```
.deps/qgis-build/output/
├── bin/
│   └── qgis.exe          # 主程序
├── plugins/              # 插件
└── python/               # Python 支持
```

### 打包输出
```
.dist/jianghe/
├── bin/
│   └── jianghe.exe       # 品牌化后的主程序
├── plugins/
├── python/
└── ...                   # 完整的运行环境
```

---

## 常见问题

### Q1: 提示 "cmake.exe not found"
**解决**：确保已正确安装 OSGeo4W 依赖，检查 `.deps/OSGeo4W/` 目录是否存在。

### Q2: 编译失败，提示语法错误
**解决**：检查修改的源代码文件编码是否为 UTF-8，确保没有引入新的乱码。

### Q3: 打包后运行提示缺少 DLL
**解决**：重新执行 `package-windows.ps1`，确保所有依赖库都被复制。

### Q4: 想要加快编译速度
**解决**：修改 `build-qgis.ps1` 中的 `-Parallel` 参数，默认为 8，可根据 CPU 核心数调整。

---

## 后续开发流程

每次修改代码后，按以下流程操作：

1. **修改源代码**
   - C++ 代码：`QGIS/src/app/ecorestoration/*.cpp`
   - 头文件：`QGIS/src/app/ecorestoration/*.h`

2. **编译验证**
   ```powershell
   .\build-qgis.ps1
   ```

3. **测试**
   ```powershell
   # 快速测试（开发版本）
   .\start-qgis.ps1
   
   # 或测试打包版本
   .\package-windows.ps1
   .dist\jianghe\启动江河系统.cmd
   ```

4. **发布**
   - 确认功能正常后
   - 将 `.dist\jianghe\` 整个目录交付给用户

---

## 版本管理建议

### 建议的版本号命名
```
.dist/
├── jianghe/              # 当前版本（始终是最新的）
├── jianghe-v1.0.0/       # 备份的稳定版本
├── jianghe-v1.0.1/       # 修复乱码后的版本
└── jianghe-v1.1.0/       # 新增水保决策功能后
```

### 发布前检查清单
- [ ] 编译成功，无警告
- [ ] 中文显示正常
- [ ] 核心功能测试通过
- [ ] 打包完整，启动脚本正常
- [ ] 修改内容已记录

---

## 技术支持

如有编译问题，请检查：
1. 错误日志输出
2. `.deps/qgis-build/` 目录下的编译日志
3. Windows 事件查看器中的应用程序日志

---

**文档生成时间**：2026-09-11  
**Claude 版本**：Opus 5
